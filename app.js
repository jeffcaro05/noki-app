(()=>{'use strict';const{questions,results,route,answer}=NokiLogic,app=document.getElementById('app');let answers=[],delivery={destination:'',checks:[]};
const b=(label,action,primary=false)=>`<button data-action="${action}" class="${primary?'primary':''}">${label}</button>`;
function show(html){app.classList.remove('screen-enter');void app.offsetWidth;app.innerHTML=html;app.classList.add('screen-enter');app.querySelectorAll('[data-action]').forEach(x=>x.onclick=()=>act(x.dataset.action));const h=app.querySelector('h1');if(h){h.tabIndex=-1;h.focus({preventScroll:true})}window.scrollTo({top:0,behavior:'smooth'})}
function home(){show(`<section class="hero"><div class="hero-copy"><div class="eyebrow">Todavía sirve, recicla</div><h1>Decide qué hacer con tu celular en desuso.</h1><p class="lead">Noki te orienta para conservarlo, entregarlo o llevarlo a un punto responsable.</p><div class="actions">${b(answers.length?'Continuar orientación':'Comenzar orientación','continue',true)}${answers.length?b('Reiniciar','restart'):''}</div></div><div class="hero-art"><img src="noki-original.png" alt="Noki, un celular antiguo ilustrado"></div></section><section class="intro-grid"><article><span class="card-number">01</span><h2>Una decisión clara</h2><p>Responde tres preguntas y recibe una orientación breve.</p></article><article><span class="card-number">02</span><h2>Información local</h2><p>Consulta puntos registrados en Medellín sin depender de internet.</p></article></section><div class="actions secondary-actions">${b('Consultar puntos','points')}${b('Preparar una entrega','prepare')}${b('Glosario','glossary')}</div>`)}
function question(i){const q=questions[i];show(`<div class="eyebrow">Orientación · Paso ${i+1} de 3</div><div class="steps" aria-label="Paso ${i+1} de 3">${questions.map((_,j)=>`<span class="${j===i?'current':''} ${j<i?'done':''}"></span>`).join('')}</div><h1>${q.title}</h1><p class="lead">${q.description}</p><div class="choices">${q.options.map(([v,l])=>`<button class="choice" data-action="answer:${i}:${v}"><span>${l}</span><span class="choice-mark" aria-hidden="true">›</span></button>`).join('')}</div><div class="actions muted-actions">${i?b('Pregunta anterior',`edit:${i-1}`):b('Volver al inicio','home')}</div>`)}
function render(){const c=route(answers);if(c.result)return result(c.result);question(c.question)}
function result(id){const r=results[id],why=id==='R1'?'Indicaste una condición que conviene revisar antes de decidir.':id==='R2'?'Aún no hay suficiente certeza sobre el funcionamiento del equipo.':'Tus respuestas permiten explorar un destino concreto.';show(`<div class="eyebrow">Resultado de tu orientación</div><h1>${r.title}</h1><p class="lead">${r.text}</p><div class="note"><strong>Por qué aparece esta orientación</strong><p>${why}</p></div><section class="next-step"><span class="card-number">Siguiente paso</span><h2>${r.next}</h2><p>Si quieres, compara las alternativas antes de cerrar una decisión.</p></section><details><summary>Ver mis respuestas</summary><ul>${answers.map((v,i)=>`<li><span>${questions[i].title}</span><strong>${questions[i].options.find(o=>o[0]===v)[1]}</strong></li>`).join('')}</ul></details><div class="actions">${b('Comparar alternativas','compare',true)}${b('Cambiar respuestas',`edit:${answers.length-1}`)}${b('Volver al inicio','home')}</div>`)}
function compare(){show(`<div class="eyebrow">Alternativas posibles</div><h1>Elige un destino que tenga sentido para tu equipo.</h1><div class="comparison"><article><h2>Conservar</h2><p>Úsalo para una función sencilla si todavía responde y lo necesitas.</p></article><article><h2>Entregar a otra persona</h2><p>Comprueba que sea útil, informa sus limitaciones y protege tus datos.</p></article><article><h2>Consultar recolección</h2><p>Confirma que el punto reciba celulares y revisa sus condiciones.</p></article></div><div class="actions">${b('Preparar una entrega','prepare',true)}${b('Volver a mi orientación','continue')}</div>`)}
function points(){const ps=[['Centro de Acopio Ambiental Monterrey','Carrera 48 #10-45 · segundo piso','Celulares, cargadores y otros residuos tecnológicos','Horario publicado en conflicto. Confirma antes de ir.','Monterrey'],['Recolección Respel El Tesoro','Carrera 25A #1A Sur-45 · transversal Superior','Equipos de telefonía y baterías de celular','7:00 a. m. a 9:00 p. m., según la ficha publicada.','El Tesoro'],['Puntos Naranja Emvarias','Santa Lucía, Laureles, Belén y otros puntos','RAEE y materiales posconsumo según Emvarias','Condiciones y coordenadas pendientes de verificación.','Emvarias']];show(`<div class="eyebrow">Directorio offline · Medellín</div><h1>Encuentra un punto registrado.</h1><p class="lead">La información está guardada en la app. Confirma las condiciones antes de desplazarte.</p><div class="map-list">${ps.map(p=>`<article class="place"><span class="place-tag">${p[4]}</span><h2>${p[0]}</h2><p><strong>Ubicación:</strong> ${p[1]}</p><p><strong>Recibe:</strong> ${p[2]}</p><p class="pending"><strong>Nota:</strong> ${p[3]}</p></article>`).join('')}</div><div class="actions">${b('Preparar mi entrega','prepare',true)}${b('Volver al inicio','home')}</div>`)}
function prepare(){show(`<div class="eyebrow">Cierre de entrega</div><h1>Deja tu equipo listo y registra su destino.</h1><p class="lead">Completa la lista. Al finalizar recibirás un código local de referencia.</p><label class="field-label" for="destination">Destino elegido</label><select id="destination"><option value="">Selecciona un punto o destino</option><option>Centro de Acopio Ambiental Monterrey</option><option>Recolección Respel El Tesoro</option><option>Puntos Naranja Emvarias</option><option>Otra persona</option><option>Conservarlo</option></select><div class="checklist"><label><input type="checkbox" data-check="1"> Protegí o retiré la información personal que necesito.</label><label><input type="checkbox" data-check="2"> Separé el equipo, cargador y batería según las indicaciones recibidas.</label><label><input type="checkbox" data-check="3"> Confirmé el destino y sus condiciones antes de salir.</label></div><div class="note"><strong>Importante</strong><p>Este registro no confirma la recepción del equipo. Lleva el código y pregunta por el procedimiento del lugar.</p></div><div id="delivery-result" class="delivery-result" hidden></div><div class="actions">${b('Generar código de entrega','generate',true)}${b('Consultar puntos','points')}${b('Volver al inicio','home')}</div>`);const s=document.getElementById('destination');s.onchange=()=>delivery.destination=s.value;app.querySelectorAll('[data-check]').forEach(x=>x.onchange=()=>{delivery.checks=[...app.querySelectorAll('[data-check]:checked')].map(y=>y.dataset.check)})}
function glossary(){show(`<div class="eyebrow">Glosario Noki</div><h1>Palabras clave para tomar una decisión.</h1><div class="comparison glossary"><article><h2>RAEE</h2><p>Residuos de aparatos eléctricos y electrónicos. Incluye celulares, cargadores y otros equipos.</p></article><article><h2>Punto de entrega</h2><p>Lugar que recibe determinados equipos o materiales bajo sus propias condiciones.</p></article><article><h2>Datos personales</h2><p>Información, fotos, cuentas o archivos que pueden identificarte y deben protegerse antes de entregar un equipo.</p></article><article><h2>Reutilizar</h2><p>Seguir usando el equipo o darle una segunda función cuando todavía es seguro y útil.</p></article></div><div class="actions">${b('Preparar una entrega','prepare',true)}${b('Volver al inicio','home')}</div>`)}
function generate(){const box=document.getElementById('delivery-result');if(!delivery.destination||delivery.checks.length<3){box.hidden=false;box.innerHTML='<strong>Completa la lista para generar el registro.</strong><p>Selecciona un destino y marca las tres verificaciones.</p>';return}const code='NOKI-'+new Date().toISOString().slice(0,10).replaceAll('-','')+'-'+Math.random().toString(36).slice(2,6).toUpperCase();box.hidden=false;box.innerHTML=`<span class="card-number">Registro preparado</span><h2>${delivery.destination}</h2><div class="delivery-code">${code}</div><p>Tu equipo está listo para llevarlo al destino elegido. Conserva este código como referencia local.</p>`;box.scrollIntoView({behavior:'smooth',block:'center'})}
function act(a){const[t,i,v]=a.split(':');if(t==='answer'){answers=answer(answers,Number(i),v);render()}else if(t==='edit')question(Number(i));else if(t==='restart'){answers=[];delivery={destination:'',checks:[]};home()}else if(t==='continue')render();else if(t==='compare')compare();else if(t==='points')points();else if(t==='prepare')prepare();else if(t==='glossary')glossary();else if(t==='generate')generate();else home()}home()})();
